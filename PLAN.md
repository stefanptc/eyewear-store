# PLAN.md

**Project:** Premium Shopify Store Build — Romanian Prescription Eyewear
**Last updated:** June 3, 2026 (session 13)
**Brand name:** **Optivix** (locked June 3, 2026 · `optivix.ro` registered) — replaces placeholder "Optic Mărășești"; theme + CLAUDE.md + admin swap tracked as an M3 task.
**Current milestone:** M5 — Polish layer (🟡 in progress, session 4 of ~5 — homepage tightened, motion polish baseline shipped, /pages/lentile + /pages/despre built, hero imagery tool switched flux→recraft; admin punch list pending Stefan's clicks; M4 still blocked on Friend's pricing logic)

---

## How this plan works

- **Milestones are exit gates, not freezes.** Any milestone can be re-opened when later work reveals a real reason to. Re-opening requires a specific trigger, not perfectionism.
- **1 session ≈ 2–3 hours** of focused work. Sizes are rough.
- **Owners:** [Me] = Stefan, [Friend] = business owner, [Together] = needs both.
- **Status legend:** ⬜ todo · 🟡 in progress · ✅ done · 🔁 re-opened
- **End of each session:** "Mn — done X, blocked on Y." Claude updates this file.

---

## Work split — Friend-gated vs. solo

Two-track view of *remaining* work (live summary; the milestone sections below stay the source of truth). Segregated session 13 at Stefan's request so solo work can proceed without waiting on Friend.

### Track A — Blocked on Friend (can't finish without him)

- **Lens-calculator pricing logic** (M4) → unblocks the entire calculator build
- **Optician partnership** (M4) → the prescription-validation half of the lens flow
- **Real product photography** (M5) → final imagery pass (placeholders hold until then)
- **Native-speaker copy review** (M5) → finalizes the RO copy already drafted; gates the EN pass
- **Shopify plan + Payments + DNS wiring** (M6) → real checkout, EUR display, live domain
- **VAT registration** (M6) → TVA tax configuration
- **Courier account/contract** (M6) → shipping rates + AWB + tracking integration
- **Supplier feed** (M6) → full catalogue + real inventory at scale
- **Company legal data** (CUI, nr. reg.) (M6) → Romanian footer obligations
- **Lawyer consult** (M6) → returns flow, cookie/legal wording, legal-page sign-off, custom-made-goods exemption
- **Trademark search** (Friend/legal, M6)
- **Store transfer** (Together, M6)

### Track B — We can do alone (no Friend needed)

*Code (Claude) — lands in the repo:*
- ✅ Optivix brand swap (hero caption) — M3 (session 13)
- ✅ `/pages/contact` editorial pass — M5 (session 13)
- PDP polish (gallery, related products, variant picker) — M5 — **needs pairing** (Claude can't see the rendered store; Stefan previews via `theme dev`, reports, Claude fixes)
- Draft RO copy (Friend reviews later) — M5
- SEO: meta tags, schema, `llms.txt`, hreflang scaffolding — M6
- Performance + accessibility passes — M6
- Legal pages — *draft only*, lawyer reviews — M6
- Cookie-consent banner install/config (final wording later) — M6
- Transactional-email RO translation + branding — M6

*Admin (Stefan's clicks) — Claude guides, Stefan clicks:*
- ✅ Bind `page.lentile` + `page.despre` templates (session 13)
- ✅ Store name → "Optivix" (Settings → General) — fixes the header logo text (session 13)
- ✅ Vendor → "Optivix" on all 5 products — also cleared the vendor cedilla (session 13)
- ✅ Currency format `RON` → `lei` (session 13)
- ✅ Inventory tracking on, 25/product — "Stoc epuizat" gone (session 13)
- Cedilla cleanup — **deferred**: fold into the real-product import (avoid throwaway on placeholders); flip collection rules + data to comma-below together. Punch list: `docs/m5-cedilla-cleanup.md`
- Replace Universităţii's watermarked image — **deferred** into the real-product imagery pass (Stefan has real photos + specs coming soon)
- Delete old US dev store (`stefan-dev-lens-store`) — pending
- Tidy theme library (delete stale drafts: Dawn M2, Dawn M5, Horizon) — pending
- Install TryOnMe try-on app — pending

---

## M1 — Foundations
*Size: ~4 sessions · Status: ✅ done (3 sessions, May 18–19)*

**Goal:** Dev environment fully wired so we can actually build.

- ✅ Shopify Partner account created
- ✅ Dev store created (Stefan dev lens store, Basic plan, RON)
- ✅ Project instructions + research PDF + this plan in place
- ✅ [Me] Install Shopify CLI, Node, Git locally (Node 20.20.2, npm 10.8.2, Git 2.43, Shopify CLI 3.94.3; npm prefix moved to `~/.npm-global` to avoid sudo)
- ✅ [Me] Install Claude Code + Shopify AI Toolkit plugin (Claude Code 2.1.143 native binary; plugin route instead of raw MCP — auto-updates, bundles 16 skills + MCP; telemetry opted out via `OPT_OUT_INSTRUMENTATION=true` in `~/.bashrc`; running Claude Code from terminal, not Cursor)
- ✅ [Me] Fork Dawn theme into a Git repo (forked `Shopify/dawn` → `stefanptc/eyewear-store`, public for now — flip to private when M4 business logic lands; cloned to `~/AI/Projects/eyewear-store`; `upstream` remote points to Shopify/dawn for future updates; SSH keys generated on this machine and added to GitHub; `shopify theme dev` confirmed working against `stefan-dev-lens-store.myshopify.com`, theme renders at 127.0.0.1:9292 with hot-reload; storefront password: `bahmah`)
- ✅ [Me] Create `docs/GIT_CONVENTIONS.md` (branching, Conventional Commits, push policy, upstream Dawn sync rules) — referenced by `CLAUDE.md`
- ✅ [Me] Create `CLAUDE.md` in repo root (project summary, Liquid version, brand voice, push policy, MCP rules) — committed `docs: add claude.md`
- ⬜ [Friend] Confirm optician partnership / regulatory status track (parallel, non-blocking)

**Exit criterion:** `shopify theme dev` runs locally, Claude Code can read the theme, first commit pushed.

---

## M2 — Design system & brand
*Size: ~5 sessions · Status: ✅ done (1 session, May 20 — faster than expected, scope shifted away from joint mood board)*

**Goal:** Visual identity locked. Every later decision flows from this.

- ✅ [Me] Built reference grid comparing 6 premium eyewear sites (Warby Parker, Ace & Tate, Cubitts, MOSCOT, Bailey Nelson, Dresden Vision) on type, colour, hero, card, motion — `docs/m2-reference-grid.html`
- ✅ [Together] Direction locked: A / A / D — serif-led editorial typography + monochrome with one ownable warm accent + type-led hero (provisional, may shift to product-as-object once real photography exists). Reference: **Cubitts**.
- ✅ [Me] Typography exploration — tested 3 serif/sans pairings on real Romanian product copy with full diacritic stress · `docs/m2-typography-exploration.html`. Locked: **Source Serif 4 × IBM Plex Sans × IBM Plex Mono**.
- ✅ [Me] Accent exploration — 7 warm candidates rendered in product context · `docs/m2-accent-exploration.html`. Locked: **Mushroom taupe #8C7A5C** (quiet-warm, whisper accent).
- ✅ [Me] Type scale + spacing kit — full homepage mock applying every token, plus reference panel · `docs/m2-kit-page.html`. Tokens live in `CLAUDE.md` section 5.
- ⏸️ [Friend] Sign-off deferred. Stefan is the design owner per session-4 agreement; Friend approves at M5 polish gate instead of every milestone.
- ✅ [Friend] **Brand name locked: Optivix. Domain registered.** (June 3, 2026 — pulled forward from M5; valid trigger: lock the name before squatters, and domain ownership is decoupled from the paid Shopify plan, so no reason to wait.) `optivix.ro` registered at **ROTLD** under the **existing** company **Pro Optic Eyes S.R.L.** ("ProOptic") — registrant is the company (Companie Comercială), **not a person**, so no future "transfer of right of use" headache. WHOIS status OK, registered 03-06-2026, 1-year term. `.ro` company contact data is public in WHOIS (expected and unavoidable for a legal-entity registrant). `optivix.com` **not acquired** — already held by a third party; decided not to pursue a `.com` alternative (Romania-only, `.ro` is the brand's home). Accepted as a known defensive gap; the third-party `.com` holder is a mild signal feeding the trademark follow-up (M6). Domain **not connected to Shopify yet** — DNS wiring deferred to M6. Theme/CLAUDE.md/admin placeholder swap is now an M3 task (below).
- ✅ [Me] Encode design tokens in CLAUDE.md section 5 — full `:root` block ready to paste into Dawn's `base.css`.

**Exit criterion met:** Kit page renders the full design system at every breakpoint (375 / 1024 / 1440 tested). Stefan signed off on type, colour, scale, spacing.

---

## M3 — Information architecture & sample content
*Size: ~4 sessions · Status: ✅ done (3 sessions, May 20–22 — exit criterion met session 7; image-replacement + dev-store-cleanup carry over)*

**Goal:** Store has structure, even with placeholder products.

- ⏸️ [Friend] Send 5–10 real product samples — **DEFERRED** per session-4 decision. Building with theatre (Unsplash stock + AI-generated generic eyewear) through M5 so design comes alive. Real product photography swaps in pre-M6 launch.
- ✅ [Me] Apply locked design tokens to Dawn's `base.css` (commit c62b917, session 5)
- ✅ [Me] **Recreate dev store as Romania-defaulted** — `lens-store-romanian.myshopify.com` (session 5). Old US-defaulted store `stefan-dev-lens-store` flagged for deletion next session. Learned: Shopify Create-store flow doesn't ask for country; every dev store needs a manual Settings → General regional fix afterward. Cascade from store-address country change handles most of it; backup region and time zone need manual correction.
- ✅ [Me] **Define frame product schema** — `docs/m3-data-model.md` drafted and committed (commit fb691cd, session 5). Captures lens-led IA (`/pages/lentile` primary, Rame secondary), product type vocabulary, frame metafields, tag conventions, collection rules, navigation skeleton, stub pages, Markets/locale config, and open M4 lens-product-modeling questions.
- ✅ [Me] **Create 9 frame metafield definitions in admin** — `custom.frame_*` namespace, all with Romanian display names + full diacritics. 4 Choice list (material, shape, colour_finish, gender_fit), 1 plain Single line text (country_origin), 4 Dimension/mm (lens_width, bridge_width, temple_length, total_width). Two doc updates after this session: Choice list is the right Shopify-native enum (replaces "deferred to metaobjects" language); Dimension fields cannot be used in smart collection rules.
- ✅ [Me] Set up collections, stub pages, navigation (session 6). **5 automated collections** (`rame`, `rame-barbati`, `rame-femei`, `rame-rotunde`, `rame-acetat`) with smart-collection rules referencing the frame_* metafields. **3 stub pages** (`lentile`, `despre`, `contact` — Contact pre-existed with Dawn's contact form template). **Main + footer nav wired** per data model §6 (lens-led: Acasă, Lentile, Rame ▾ {Pentru bărbați, Pentru femei}, Despre, Contact in main; Returnări / Confidențialitate / Termeni / Contact in footer). Romanian policy pages initialized via "Insert template" so footer links resolve (full lawyer-reviewed text is M6 work). Product templates left at Dawn defaults — customization is M5.
- ✅ [Me] **Mid-session schema fix: `frame_gender_fit` switched from Choice list to List of choices** (commit 2ae3543). Smart-collection rules don't support mixed AND/OR, so the original `product_type = Ramă AND gender ∈ {bărbați, unisex}` shape wasn't expressible. With a list metafield, the rule simplifies to `product_type = Ramă AND gender_fit contains bărbați` (clean AND), and a "unisex" frame carries both `bărbați` and `femei` values in its list. Doc + admin both updated. `unisex` is gone as a discrete value.
- ✅ [Me] **Push Dawn fork to dev store, publish live** (session 6). When `lens-store-romanian` was created session 5, Shopify auto-installed **Horizon** as default (Shopify changed the default theme in 2025). Our local Dawn fork with M2 design tokens had never been pushed — was running invisible. Pushed via `shopify theme push --unpublished -t "Dawn M2 (in dev)"`, theme ID 155087208621, then published via admin. Horizon now in Draft themes library. Decision rationale recorded: Dawn stays per CLAUDE.md §1 — LLM reliability, sunk M2 cost, restrained design language all align. M2 *visual* design isn't fully landed yet (tokens defined but not yet wired into Dawn's section CSS — that's M5 work); structural skeleton is what M3 delivers.
- ⏸️ [Me] ~~Configure metafields for lens parameters (sphere, cylinder, axis, PD, lens type, coatings) — namespace `custom.lens_*`~~ — **moved to M4.** Discovered during session 5 that these are *customer-prescription* data (sphere/cylinder/axis/PD), not product attributes — they live on customer/cart/order, not products. Architecture decided in M4 (see `docs/m3-data-model.md` §10 open question 1). The `custom.lens_*` namespace stays reserved; no fields created yet.
- ✅ [Me] Build out core pages: home, collection, product (Romanian content first; English deferred to M5). Stub pages + working collection/product templates are live; rich content + theme rewire to apply M2 design tokens is M5 polish work.
- ✅ [Me] Source Unsplash placeholder product shots — generic eyewear, neutral backgrounds. **5 of 5 sourced** (Mărășești tortoise round session 6; Aviatorilor metal aviator, Universităţii tortoise cat-eye, Victoria titan rectangular, Lipscani transparent round session 7). **Carryover:** Universităţii's photo is a watermarked iStock preview — must be replaced with a free Pexels/Unsplash equivalent before any storefront sharing.
- ✅ [Me] **Create placeholder frame products** — all 5 created and verified. **Mărășești** (session 6) created in admin, validated the full schema. **Aviatorilor, Universităţii, Victoria, Lipscani** (session 7) imported via **CSV bulk import** — used a Mărășești export as the column-format template, drafted a 4-row CSV, imported clean on first try. All 5 products auto-populated the correct smart collections: `Toate ramele=5`, `Rame bărbaţi=4`, `Rame femei=3`, `Rame rotunde=2`, `Rame din acetat=3`. Photos uploaded manually per product (Image Src left blank in CSV deliberately). **The CSV pipeline is the canonical path for Friend's eventual supplier-feed import.**
- 🟡 [Me] Configure Shopify Markets — RON as store/settlement currency ✅ (session 5). **EUR display deferred to M6** — Shopify Markets multi-currency requires Shopify Payments setup, which requires Friend's business entity. See CLAUDE.md §1.
- ✅ [Me] Install Translate & Adapt + add EN locale empty (session 5) — both auto-handled by Shopify when store address country was set to Romania. Romanian primary/published, English added with no translations (content lands M5).

- ✅ [Me] **Swap brand placeholder "Optic Mărășești" → "Optivix"** (session 13 — complete). Brand lived in three live spots, all now Optivix: theme hero caption (`templates/index.json`), the **store name** (Settings → General — drives the header logo text), and the **vendor** field on all 5 products (which also cleared that field's cedilla). No smart-collection rule references the brand (collections key off `product_type` + `frame_*`, not vendor), and CLAUDE.md never contained the placeholder. The **Mărășești** *frame product* (Bucharest-street name) was correctly left alone. A proper logo *image* remains a future brand-asset task.

**Exit criterion met (session 7):** Click-through prototype works end-to-end on `lens-store-romanian.myshopify.com` — Toate ramele / Pentru bărbați / Pentru femei collections render with correct counts, product pages resolve with photo + price + Romanian UI, footer policy links work, nav dropdown navigates via `Vezi toate` workaround. EN locale present, empty (M5 content). Design tokens defined in `:root` but full Dawn section-CSS rewire is M5 polish work (per session 6 scope clarification — structural skeleton is what M3 delivers). EUR display gated on M6 Shopify Payments.

---

## M4 — Custom feature: lens recipe calculator
*Size: ~6–8 sessions · Status: 🟡 in progress (Steps 0–5 shipped; Step 5 add-to-cart wired S21) · Hardest milestone*

**Goal:** Customer enters prescription, sees correct lens + price — and the prescription gets validated before fulfillment (legal hard rule, CLAUDE.md §7).

- ✅ [Together] Friend documents pricing logic: which lens types, which coatings, how index/material affect price — **done 2026-06-10:** `docs/lens-pricing-matrix.md` v1 (30 priced rows, 4 non-blocking TBDs open)
- ✅ [Together] **Decide the lens product model** — how a frame + lens recipe becomes a cart line at the right price (Shopify variants vs line-item properties vs draft order). **Locked 2026-06-10: (b) variant-per-matrix-row** — see `docs/m3-data-model.md` §10 (OQ1/OQ3/OQ5 resolved).
- ✅ [Me] Build calculator UI in a Liquid section + JS — **done 2026-06-12 (S17):** `sections/lens-calculator.liquid` + `lens-calculator-ui.js` + `lens-calculator.css`, a `<lens-calculator>` web component rendering the proven engine's result object; on `/pages/lentile`. UI shell only (no SVG preview / add-to-cart — Steps 4–5). Acceptance-tested S17.
- ✅ [Me] **Step 4 — lens preview + combo-disabling** (S18). Two-panel parametric SVG preview (front view + side cross-section) painted from the calculator's **single render path**, tokens-only, reduced-motion-safe, with try/catch degradation so the preview can never break the calculator. Added under the same render path: **combo-disabling** — every segmented option (type/coating/light/index) is probed against the pricing engine each recompute and disabled when it yields no offer; it **never disables the current selection**, and disables **none** when a whole group would go dark (the engine error surfaces instead). SR hint (`option_unavailable`) on disabled options. Plus the **provisional-price marker** ("preț provizoriu") restyled down to mono microcopy scale. All gates green (`theme check --fail-level=error` + Dev MCP `validate_theme` on every changed file). Infra (S18): `.shopifyignore` added — ignores `*.tmp.*`, killing the S17/S18 theme-dev watcher-race incident class; Shopify Dev MCP is now project-scoped via `.mcp.json`. **S19 — acceptance checks cleared + display-robustness pass (45fa3ab):** verified Rx-driven disable/re-enable and no-yank (a selection is never disabled out from under the user); then lifted border-token contrast (`--rule`/`--rule-faint`) and replaced the `opacity:0.4` disabled dimming with a legible fill/shape three-state (`--ink-disabled` text + `--fill-disabled` tile) so controls hold up on uncalibrated/bright monitors (WCAG 1.4.11) — a per-group reason-note experiment was tried and dropped (the clear disabled styling carries the signal). **Step 4 is now functionally complete; single remaining thread to fully close it: formal sign-off of the M18 „preț provizoriu” provisional-price marker (C).**
- ✅ [Me] **Step 5 — add-to-cart wiring** (S21, commit f28b34d). The chosen result POSTs the matched variant to `/cart/add.js` with the prescription as **visible** line-item properties (grouped per-eye SPH/CYL/AX + PD, plus ADD for bifocals). rowId→variant join is **SKU = matrix row id** (`docs/m4-lens-variant-metafields.md`), emitted from Liquid as a JSON map via a new `lens_product` product-picker section setting; empty map (product not yet Active/selected) degrades the button to a "not yet available" message. A **PD field** was added to the Rx step (fulfillment data only — never an engine input; frozen engine/data/tests intact, 217/217). Repaints Dawn's `cart-notification` in place. Gates green (Dev MCP `validate_theme` VALID ×5, `theme check` 0 errors). **Pending live QA by Stefan** + the admin prereqs below.
- ⬜ [Me] Wire to metafields, validate edge cases, handle invalid prescriptions
- ⬜ [Me] Add calculator entry from the product/frame page flow
- ⬜ [Stefan-admin] **Build the lens product** (Draft→Active + Online Store channel; 34 variants, SKU = row id; variant metafields per `docs/m4-lens-variant-metafields.md`) and select it in the calculator section's `lens_product` setting → populates the cart map.
- ⬜ [Together] **Prescription upload + optometrist validation flow** — customer attaches their actual prescription (file upload / line-item property), and Friend's licensed optometrist validates it **before** the order is fulfilled. CLAUDE.md §7 hard rule: no glasses ship without this step. Build = the upload mechanism + an order-hold/validation status; Friend = the optician partnership that does the validating (the M1 regulatory track lands here).

**Exit criterion:** End-to-end test — enter a real prescription, get correct product variant in cart at correct price, prescription captured and flagged for validation. Friend signs off on the math.

---

## M5 — Polish layer
*Size: ~5 sessions · Status: 🟡 in progress (session 3 of ~5 done — session 10 of project)*

**Goal:** Store feels premium, not "AI-generated."

- ✅ [Me] **Land M2 design system into Dawn** (session 8). Google Fonts loading Source Serif 4 + IBM Plex Sans + IBM Plex Mono with Romanian latin-ext subset; Dawn's `--font-*-family` rewired to M2 families in `assets/base.css`; all 5 colour schemes in `config/settings_data.json` rewritten as mushroom palette (scheme-1 mushroom default, scheme-2 shaded, scheme-3 dark inverse, scheme-4 white card, scheme-5 accent). Cards now show italic Source Serif 4 names + IBM Plex Mono mushroom-muted `material · width mm · colour` spec line driven by `custom.frame_*` metafields + mono prices (via `| metafield_text` filter — bare `{{ metafield }}` of a Dimension serializes to JSON). PDP title italic Source Serif 4 + mono price for consistency with cards.
- ✅ [Me] **Replace cartoon hero with editorial rich-text section** (session 8). `templates/index.json`: `image_banner` (image-banner type, Dawn placeholder hippies-at-a-lake cartoon) swapped for `hero` (rich-text type). Blocks: caption "OPTIC MĂRĂȘEȘTI" rendered as mono mushroom-muted uppercase eyebrow + heading "Rame pentru vedere." in italic Source Serif 4 at hxxl + body "Cinci modele alese cu grijă. Lentile pe rețetă, montate corect." + CTA "Vezi ramele" → `/collections/rame`. 200 px vertical breathing room. Validated by Stefan's wife (web designer) and a friend — direction confirmed.
- ⬜ [Me] **AI-generated frame exploded view hero** (pending Stefan's Recraft setup). Hodinkee/Rado-style horizontal exploded illustration, mushroom monochrome. **Tool switched session 11: Flux 1.1 Pro → Recraft** for palette-locked editorial illustration (Brand Kit holds mushroom hex codes, Style Lock derives reusable Style ID from uploaded refs, native SVG output). See updated `memory/project_hero_imagery_plan.md` + CLAUDE.md §1. Flux retained in stack for photoreal post-M6. Backup tool: Nano Banana Pro (Gemini 3 Pro Image) via Google AI Studio. Lens exploded follows on `/pages/lentile` after.
- ✅ [Me] **Translate Dawn placeholder copy → Romanian** (session 9, commit 2c0cfba). Announcement bar, `index.json` brand/featured/collage/3-column blocks, and `product.json` accordion titles + image-with-text + columns + related/share now read Romanian (comma-below diacritics, "tu" address). Two judgment calls: product "Free Shipping" / "Hassle-Free Exchanges" columns **repurposed** to policy-free value props (shipping/returns unconfirmed; returns touch §7 law), and body filler rewritten as short on-brand draft copy with no fabricated specifics. Empty **collage** + random-YouTube **video** sections left for the layout pass (structural, not copy). All draft pending Friend's native-speaker review. EN translations come later via Translate & Adapt.
- ✅ [Me] **Wire `custom.frame_*` metafields into PDP Materials/Dimensions accordions** (session 9, commit d933695). Added an opt-in `frame_specs` select (`none`/`materials`/`dimensions`) to Dawn's `collapsible_tab` block in `sections/main-product.liquid` — reuses Dawn's `<details>` markup, no new block types. When set it renders a `<dl class="frame-specs">` of the relevant fields via `| metafield_text`, each row guarded by `!= blank`. Materiale → material/colour/origin; Dimensiuni → the four mm measurements (rendered `NN mm`). Keys off a real setting, not heading text, so it survives renames + the EN locale. Mono editorial styling in `base.css`. Closes the "I expect to see dimensions here" gap from session 8 Image 7. Validation: `theme check` clean (0 errors) — the shopify-liquid skill's own `validate.mjs` is unrunnable here (missing dep), so `theme check` is the §4 gate (saved to memory).
- ✅ [Me] **Make dropdown/drawer parent menu items navigable** (session 10, commits 264b7e7 + 88f4b30). Data-model §6 carryover. Dawn renders a parent-with-children as a `<summary>` toggle that ignores `link.url`, so `Rame` couldn't navigate to `/collections/rame` (the `Vezi toate` first-child workaround). Patched `snippets/header-dropdown-menu.liquid` (desktop) + `snippets/header-drawer.liquid` (mobile): the parent label is now an `<a href="{{ link.url }}">` nested in the `<summary>`, while the caret/row stays the disclosure toggle. Inner `<span>` kept so Dawn's hover/active/open underline CSS still applies; `.header__menu-parent-link` / `.menu-drawer__parent-link` reset the default link colour/underline (`base.css`). `theme check` clean. `Vezi toate` is now optional (admin) — both viewports reach the collection without it.
- ⬜ [Me] Final product imagery pass (Photoroom cleanup of real product shots, Flux for non-frame heroes)
- ⬜ [Me] Romanian copy pass (primary): bulk via Shopify Magic → re-tone with Claude → friend reviews as native speaker
- ⬜ [Me] English copy pass (secondary, for tourists/expats in Romania): translate finalized Romanian content via Translate & Adapt + Claude tone pass; UX courtesy, not an international storefront
- ⬜ [Me] Install virtual try-on app (TryOnMe to start), configure on product pages
- ✅ [Me] **Microinteractions, transitions, loading states** — baseline session 11 (commit 6ba7bd4): killed Dawn's product-card scale-on-hover (transforms forbidden per §5; replaced with single-image opacity 0.92 dim, two-image opacity swap retained), brand-aligned `*:focus-visible` ring using `currentColor` so contrast holds across all 5 schemes, universal `prefers-reduced-motion` killswitch stronger than Dawn's class-based opt-in. **Tail closed session 12 (commit 7fb4ff5):** add-to-cart label now fades to transparent as loading engages (`color` added to the button transition, redeclared with Dawn's box-shadow hover so it isn't dropped) + `cursor: progress` in-flight; universal `:active` opacity-0.9 press feedback across add-to-cart/contact/newsletter submits (Dawn's only press dip was gated behind the unused `.animate--hover-vertical-lift` scale); bare `.link` now thickens its underline on hover like `.underlined-link`/RTE links, which Dawn left inert. Colour/opacity only; `theme check` clean.
- ✅ [Me] **Homepage layout pass** (session 11, commit a73034f). Deleted unconfigured `video` (Dawn placeholder YouTube) and empty `collage`; replaced collage with `image-with-text` lens callout pointing at `/pages/lentile`; linked `multicolumn` columns out to `/pages/lentile` / `/collections/rame` / `/pages/contact` with quiet mono labels. Homepage now: hero → brand statement → frame grid → lens callout → linked value props. Five sections, each earning its place.
- ✅ [Me] **`/pages/lentile` editorial page built** (session 11, commits 1a87c63 + 962d42a fix). `templates/page.lentile.json` — six sections composing Dawn primitives: hero / 4-up process / index explainer / 3-up treatments / FAQ collapsible-content / CTA. Returns FAQ deliberately omitted pending Friend's lawyer review (§7). **Admin click pending:** Pages → Lentile → Theme template → page.lentile → Save.
- ✅ [Me] **`/pages/despre` brand-story page built** (session 11, commit 13749b0). `templates/page.despre.json` — five sections: hero / positioning / 3-up Cum lucrăm / Brașov-place / CTA. Zero fabricated facts (no founding year, no founder names, no product count, no brand name surfaced inline). Flagged for Friend's pass: competitive-swipe sentence in positioning, walk-in confirmation, whether to surface his name. **Admin click pending:** Pages → Despre → Theme template → page.despre → Save.
- 🟡 [Me] Cedilla → comma-below diacritic cleanup across products + collection rules (M3 session-7 carryover). **Theme files audited ✅ clean** (session 10) — only `locales/tr.json` (Turkish, correct) has cedilla codepoints; `ro.json` + all session-9 copy are comma-below. **Admin side scoped, not yet executed**: exact before→after punch list in `docs/m5-cedilla-cleanup.md` (commit 39fa941). Pending Stefan's admin clicks — ⚠️ the `bărbați` metafield value + `rame-barbati` rule must flip together (codepoint-exact match) or the collection empties. **Decision (session 13): defer.** Real products with photos + specs are coming soon from Stefan; the cedilla lives almost entirely in the 5 placeholder products, so hand-fixing them now is likely throwaway. Instead, bake comma-below into the real-product import and flip the collection rules at the same moment (rules + data together). The vendor field was already corrected via the Optivix rename.
- ✅ [Me] Change Shopify currency format `RON` → `lei` (session 13). Set all four format fields to `{{amount_with_comma_separator}} lei` / `{{amount_with_comma_separator}}` — Romanian dot-thousands, comma-decimals; prices now read `199,00 lei`.

**Exit criterion:** Friend shows it to 3 people who aren't us; they describe it as "looks like a real brand." Both RO and EN locales feel polished.

---

## M6 — Launch readiness
*Size: ~6–8 sessions (re-scoped session 13 — was ~4; the operational + Romanian-legal reality of a real store is bigger than first counted) · Status: ⬜ not started*

**Goal:** Transferable to friend's paid plan and ready for real customers.

**Site quality:**

- ⬜ [Me] Performance pass: Lighthouse ≥85 mobile, image lazy-loading, CSS minification
- ⬜ [Me] Accessibility pass: keyboard navigation, alt text, color contrast
- ⬜ [Me] SEO pass: meta tags, schema markup, `llms.txt`, hreflang (`ro-RO` primary, `en-RO` secondary — region is RO for both, not en-US/en-GB)

**Operational plumbing (makes a real order actually complete):**

- ⬜ [Together] **Shipping + courier integration** — (a) configure the shipping rates the customer sees at checkout; (b) connect a Romanian courier (Fan Courier / Sameday / Cargus) via app so orders push to the courier, AWB labels print, and tracking flows back to the customer. Needs Friend's courier account/contract.
- ⬜ [Together] **Inventory** — turn on stock tracking (fixes the "Stoc epuizat" placeholder state on the 5 products), set real quantities. At catalogue scale this syncs from Friend's supplier feed via the same CSV pipeline proven in M3.
- ⬜ [Friend] **Tax (TVA) configuration** — set up the 19% TVA in Shopify so tax is calculated and recorded correctly on orders (needs Friend's VAT registration). Distinct from the "prices display TVA included" copy rule already in place.
- ⬜ [Me] **Transactional emails — Romanian + branded** — Shopify's order / shipping / refund confirmation emails ship in generic English; translate to RO and apply brand styling.
- ⬜ [Me] **Analytics** — Shopify analytics + optional Google Analytics 4, loaded only **after** cookie consent (below).

**Legal / compliance (Romania — confirm all at the lawyer consult):**

- ⬜ [Me] Legal pages drafted (privacy, terms, returns, cookies) — flagged for legal review
- ⬜ [Me/legal] **Cookie consent banner (GDPR)** — EU/Romania requires consent before non-essential cookies/analytics load. App or theme block; wording reviewed at the consult.
- ⬜ [Together] **Romanian e-commerce footer obligations** — display company identification (denumire, CUI, nr. reg. comerțului, sediu, contact) + **ANPC** and EU **SOL/SAL** dispute-resolution links. Mandatory for RO online stores.
- ⬜ [Together] **Returns/refunds operational flow** — not just the policy text: how a customer initiates a return and how refunds are processed (14-day withdrawal right; prescription lenses may be exempt as custom-made — lawyer confirms).

**Business setup, legal sign-off, handover:**

- ⬜ [Friend] Pay for Shopify plan, set up Shopify Payments, **wire `optivix.ro` DNS to Shopify** (domain already registered at ROTLD under ProOptic, June 3, 2026 — only the DNS connection remains, not a purchase)
- ⬜ [Friend/legal] **OSIM + EUIPO trademark search on "Optivix"** before the brand goes anywhere public (the third-party-held `optivix.com` is a mild signal worth checking against)
- ⬜ [Friend] **One-hour consult with a Romanian e-commerce lawyer** (non-negotiable)
- ⬜ [Together] Transfer dev store to friend's account
- ⬜ [Me] Final QA on production env

**Exit criterion:** Order one product end-to-end as a real customer. It works. We launch.

---

## Parking lot (deferred decisions, not blockers)

- Camweara upgrade trigger: VTO usage clears ~29% adoption benchmark post-launch
- Headless / Hydrogen migration: not before 10k monthly orders (2027+ question)
- Agentic Storefronts / UCP: re-evaluate when Shopify rolls it out in EU
- v0 Premium upgrade: only if free $5/mo credit runs out in real ideation work
- Claude Pro → Max upgrade: only if hitting usage limits 2x+ per week
- Newsletter / email marketing destination (Shopify Email vs Klaviyo) — Dawn has a footer signup box, but where those addresses go is a post-launch decision

---

## Change log

- **2026-06-12 (S17):** Step 3 done; card type + price aligned to kit (584827c, 9361743, f050c56); decisions in Notion.
- **June 3, 2026 (session 13, admin)** — **Guided admin pass: recent work pushed live, brand + store config landed.** Walked Stefan through the Track-B admin checklist click-by-click (his preferred step-by-step mode). **Key unblock: the live dev store was stale.** The published theme was "Dawn M2 (in dev)" (May 21) — none of sessions 8–12 (fonts, palette, hero, /lentile, /despre, /contact, Optivix, PDP fixes) were actually on the store; Stefan had only ever seen them via local `theme dev`. Pushed current local theme via `shopify theme push --unpublished -t "Optivix M5 (in dev)"` (#155429372077), Stefan previewed + **published** it via admin (the §8-compliant manual-promote, not `--live`). Live storefront now reflects all current work — confirmed visually (OPTIVIX eyebrow, italic serif hero, editorial /lentile all rendering correctly). **Admin items completed:** (1) bound `page.lentile` + `page.despre` templates (the dropdown only listed them *after* the publish — confirming the push was the real prerequisite); (2) **store name → Optivix** (Settings → General) — fixes the header logo text that still read "Lens-store-romanian"; (3) **vendor → Optivix** on all 5 products (also cleared the vendor's cedilla in one move); (4) **inventory tracking on, 25/product** — "Stoc epuizat / Sold out" gone; (5) **currency format `RON` → `lei`** — all four format fields set to `{{amount_with_comma_separator}} lei` (Romanian dot-thousands/comma-decimals). With store-name + vendor + hero all flipped, the **Optivix brand swap is complete** (M3 task ✅) — only a future logo *image* remains. **Two deferral decisions (avoid throwaway):** (a) **cedilla cleanup deferred** — real products with photos + specs are coming soon from Stefan, and the cedilla lives almost entirely in the 5 placeholder products; fold the comma-below fix into the real-product import and flip collection rules + data together then, rather than hand-editing soon-to-be-replaced placeholders; (b) **watermarked Universităţii image deferred** into the real-product imagery pass for the same reason. **New info:** Stefan will supply real product photos **and specs** soon (sooner than the M6 assumption — unblocks the imagery + cedilla work earlier; timeline not pinned). **Still-pending solo admin:** delete old US dev store `stefan-dev-lens-store`, tidy stale draft themes (Dawn M2/M5, Horizon), install TryOnMe. **Surfaced constraint (now in the two-track split):** PDP *visual* polish needs pairing — Claude can't see the password-gated rendered store, so Stefan previews + reports and Claude fixes specifics rather than guessing blind. No commits this sub-session beyond docs (all changes were admin/store-side). **M4 still blocked on Friend's pricing logic.**
- **June 3, 2026 (session 13, build)** — **Solo code wins cleared while Friend-gated work waits.** After the planning split, knocked out the Track-B code items that can be done without Friend or live preview. Four commits to `main`: **(1) `chore` 1d4a536 — Optivix homepage swap.** Grep confirmed the brand placeholder lived in exactly one theme line (`templates/index.json` hero caption `OPTIC MĂRĂȘEȘTI` → `OPTIVIX`); no smart-collection rule references the brand (they key off `product_type` + `frame_*`). Vendor field (5 products) is Stefan's admin click and also clears that field's cedilla — cedilla doc vendor row updated to target `Optivix` directly. CLAUDE.md doesn't contain the placeholder. M3 swap task flipped ⬜→🟡 (theme done, vendor admin pending). **(2) `fix` 5f6ca11 — PDP empty-accordion cleanup.** Confirmed via `sections/main-product.liquid:212` that Dawn renders `collapsible_tab` `<details>` unconditionally — no empty-content guard — so the two blank accordions (`Întreținere`, `Livrare și retur`) showed as clickable-blank rows. Filled `Întreținere` with safe Romanian care copy (§7-allowed educational content: cleaning, storage, heat); **removed** `Livrare și retur` from the PDP until M6 (can't draft returns text before the lawyer reviews the custom-made-goods exemption, §7) — re-add then. **(3) `feat` b7ad46c — `/pages/contact` editorial intro.** `page.contact.json` already drives the live Contact page (no admin bind needed, unlike /lentile + /despre). Replaced bare `main-page` + form with a brand-statement rich-text intro (`CONTACT` eyebrow / `<em>Scrie-ne.</em>` / short body) above the form, matching /lentile + /despre. **No contact specifics invented** — real email/phone/hours aren't set until M6 (brand email needs DNS); the form collects messages. **(4) `docs` — this PLAN entry + the two-track split (7681582) + M6 scope expansion.** All theme commits `shopify theme check --fail-level=error` clean (0 errors, same 12 pre-existing Dawn warnings); JSON `json.load`-validated before each commit. **Constraint surfaced for the record:** Claude can't see the rendered storefront (password-gated, `theme dev` needs Stefan's TTY) — so *visual* polish (PDP gallery/variant/related-products look, layout judgment) must be **paired** (Stefan previews via `theme dev`, reports what's off, Claude fixes specifics), not done blind. That's why this build batch stuck to deterministic/structural fixes. **Stefan's admin checklist handed off (Track B, his clicks):** bind `page.lentile` + `page.despre` templates; vendor → `Optivix` on 5 products; inventory tracking on (fix "Stoc epuizat"); replace Universităţii watermarked image; delete old US dev store. **Held for guided walkthroughs** (gotchas): cedilla cleanup (codepoint-exact, `bărbați`+`rame-barbati` flip together), currency `RON`→`lei` (needs exact format string), TryOnMe install (app decision). **Remaining solo code after today is mostly paired-visual or deferred** (legal drafts, cookie consent, transactional emails, perf/a11y — better near launch). **M4 still blocked on Friend's pricing logic.**
- **June 3, 2026 (session 13, cont.)** — **Scope gaps closed: PLAN now reflects the full reality of a real store.** Planning conversation (Stefan's first site) — walked the whole build in plain terms and hunted for missing pieces. Added to the plan: **M4** gained the lens product-model decision (was buried as a §10 open question) and the **prescription upload + optometrist validation flow** (CLAUDE.md §7 legal hard rule — no glasses ship without an optometrist validating the real prescription; pairs with the calculator, not separate). **M6** gained a whole operational + legal layer it was missing: **shipping + Romanian courier integration** (Fan Courier/Sameday/Cargus — rates + AWB + tracking), **inventory as a real system** (was only a "fix Stoc epuizat" note), **TVA tax configuration** (distinct from the display-copy rule), **Romanian-branded transactional emails**, **analytics**, **cookie-consent banner (GDPR)**, **Romanian footer legal obligations** (company identification CUI/reg + ANPC + SOL/SAL links — mandatory for RO online stores), and the **returns/refunds operational flow** (not just the policy page). Parking lot gained the newsletter/email-marketing destination question. **M6 re-scoped ~4 → ~6–8 sessions** and reorganized into four groups (site quality / operational plumbing / legal-compliance / business-setup+handover) — the launch milestone was badly under-counted; the invisible operational + Romania-legal work is most of it. No code touched. **Takeaway for Stefan:** the build splits into *shop window* (mostly done), *the lens calculator + prescription flow* (hard, Friend-gated), and *the plumbing* (payments/shipping/inventory/tax/legal — mostly config + Friend, but it's what makes a real order legal and deliverable).
- **June 3, 2026 (session 13)** — **Brand name locked: Optivix; domain registered, pulled forward from M5.** "Talk first" session — no theme code, one docs update (this PLAN edit). **Decision:** lock the name now rather than at M5 as originally planned. Trigger is real, not perfectionism: lock the name before squatters, and domain ownership is decoupled from the paid Shopify plan (M6/Friend), so there was no dependency forcing the wait. **Registration:** `optivix.ro` registered at **ROTLD** under the **existing** company **Pro Optic Eyes S.R.L.** ("ProOptic"), 1-year term, WHOIS status OK, registered 03-06-2026. Key structural win: the registrant is the **company** (Companie Comercială), **not an individual** — so there's no future "transfer of right of use" step when the brand formally sits under the business. Trade-off accepted: `.ro` registrant contact data is **public in WHOIS** (unavoidable for a legal-entity registrant). `optivix.com` was **already taken** by a third party — decided **not** to pursue a `.com` alternative (Romania-only storefront, `.ro` is the brand's home); logged as a **known defensive gap**, and the fact that someone already holds the `.com` is a mild signal feeding the trademark check below. Domain is **not yet connected to Shopify** — DNS wiring deferred to M6 (Friend's plan/Payments track). **Follow-ups recorded in the plan:** (1) **swap placeholder "Optic Mărășești" → "Optivix"** across theme + `CLAUDE.md` + admin (vendor field, collection rules) — added as a new **M3** task (⬜), scope-guarded to rename the *brand* only, not the **Mărășești** *frame product*, and to be done in one admin pass alongside the cedilla cleanup; (2) **OSIM + EUIPO trademark search on "Optivix"** — remains **Friend/legal's** call before the brand goes public, added to **M6**. **Plan edits:** M2 brand-name item flipped ⏸️→✅ (was deferred to M5); M6 domain line reworded from "buy/transfer domain" to "wire `optivix.ro` DNS" (registered, connection-only remaining); M6 trademark item added; header now carries the locked brand name. **Unchanged:** M4 (lens calculator) still blocked on Friend's pricing logic + lens product-modelling decision (`docs/m3-data-model.md` §10); M5 polish carryovers stand (Recraft hero generation, pending admin clicks — bind page.lentile/page.despre templates, cedilla punch list, currency `RON`→`lei`; Friend's native-speaker review across session 9 + 11 copy). **Next concrete step:** Stefan's call — likely the Optivix placeholder swap (now unblocked) or the Recraft hero pass.
- **June 2, 2026 (session 12)** — **M5 polish: microinteractions tail closed.** Short session, one commit to `main` (7fb4ff5). Picked up the `🟡` microinteractions item left from session 11's motion baseline (6ba7bd4) — the three pending sub-items were add-to-cart loading polish, form-submit states, and a link-hover audit. Audited Dawn's existing button/link behaviour first: the add-to-cart spinner already works (stroke inherits `--color-button-text`, brand-aware) but the label *snaps* to transparent with no fade, and the in-flight cursor gives no async signal; buttons have **no** universal `:active` press feedback (Dawn's only `:active` dip is gated behind `.animate--hover-vertical-lift`, a scale transform we don't use, so contact/newsletter/add-to-cart submits give zero tactile press); and Dawn thickens the underline on hover for `.underlined-link`/RTE links but leaves bare `.link` inert. One cohesive block appended to the `assets/base.css` M2 motion section, colour/opacity only per §5: **(a)** added `color` to the `.button` transition (redeclared alongside Dawn's `box-shadow` hover transition so it isn't dropped) so the label fades into the loading state + `cursor: progress` on `.button.loading`; **(b)** universal `:active { opacity: 0.9 }` across add-to-cart / contact / newsletter submits; **(c)** `.link:hover` now thickens its underline to 0.2rem with shared easing, matching `.underlined-link` (`link-with-icon` carries `text-decoration:none` so it's a no-op there — safe). The spinner's rotate stays the one explicit functional transform; reduced-motion killswitch already covers the new transitions. `shopify theme check --fail-level=error` clean — 0 errors, same 12 pre-existing Dawn warnings. PLAN microinteractions item flipped 🟡→✅. **Next session: talk first** — Stefan wants to discuss direction before building. Likely topics: Recraft hero generation (still the top carryover, needs Stefan's signup + uploads per `memory/project_hero_imagery_plan.md`), the pending admin clicks (bind page.lentile + page.despre templates; cedilla punch list in `docs/m5-cedilla-cleanup.md`; currency `RON`→`lei`), PDP polish, or `/pages/contact` editorial pass. **Friend's native-speaker review still pending** across session 9 + session 11 Romanian copy. **M4 (lens calculator) still blocked on Friend's pricing logic** + lens product-modelling decision (`docs/m3-data-model.md` §10).
- **May 28, 2026 (session 11)** — **M5 polish: homepage tightened, two editorial pages built, motion baseline + hero tool switched.** Six commits to `main`. **(1) Hero imagery tool switched: Flux 1.1 Pro → Recraft (commit d71b7b0, docs only).** Stefan asked which AI image generator to actually use given how fast the field moves; dispatched a research agent to compare current state of Flux / Recraft / Ideogram / Midjourney / Nano Banana Pro / Imagen as of May 2026. Verdict for *this specific task* (editorial product schematic, palette-locked, reference-driven, premium catalogue register): **Recraft V3/V4 via Recraft Studio is purpose-built and meaningfully ahead of Flux** — its Brand Kit holds the mushroom palette (#FAF8F3 / #1A1A17 / #8C7A5C) as upload-able swatches that stick across regens (Flux drifts to navy/teal on ~30% of generations), Style Lock derives a reusable Style ID from uploaded references (the Rado True Thinline poster + 2-3 Hodinkee schematic refs in our case), and native SVG output stays sharp at every viewport. Flux **retained in the stack for photoreal post-M6** (product still-lifes, lifestyle shots) — demoted, not deleted. Backup if Recraft's Style Lock can't grab the Hodinkee register tightly enough: Nano Banana Pro (Gemini 3 Pro Image, shipped Nov 2025) via Google AI Studio. Cost: ~$0.04/raster, $0.08/SVG, budget ~$1–2 total to dial in the hero across ~10 iterations. CLAUDE.md §1 stack line updated; `memory/project_hero_imagery_plan.md` updated with the Recraft workflow + Nano Banana fallback + 2026-05-28 decision-shift annotation. **(2) Homepage layout pass (commit a73034f).** Session 9's change log flagged two unconfigured sections in `templates/index.json` — `collage` (three empty blocks) and `video` (Dawn's stock random-YouTube placeholder, `_9VUPq3SxOc`) — as "structural keep/configure/remove decisions for the imagery/layout pass." Resolved both: **deleted `video`** (no assets, no purpose, premium minimal doesn't reach for video on the homepage), **replaced `collage` with an `image-with-text` lens callout** pointing at `/pages/lentile` — the lens-led IA had no homepage surface until now, fixed. The callout uses caption + italic heading + body + button blocks (same pattern as the hero), with `desktop_content_position: middle` and a large image slot Stefan fills in admin when artwork lands. Also **linked `multicolumn` columns** that were previously dangling info blocks: column 1 → `/pages/lentile` ("Cum funcționează"), column 2 → `/collections/rame` ("Vezi colecția"), column 3 → `/pages/contact` ("Scrie-ne"). **Resulting homepage:** hero → brand statement → frame grid → lens callout → linked value props. Five sections, each earning its place. **(3) Motion polish baseline (commit 6ba7bd4).** Audited Dawn's existing transitions/focus styles. Significant offender found: Dawn ships `transform: scale(1.03)` on product card hover, 500ms — per CLAUDE.md §5 transforms are forbidden ("Only color/opacity transitions"). Three CSS additions to `assets/base.css`: **(a) override product-card scale-on-hover** with `transform: none`, keep the two-image opacity swap (product utility), add `opacity: 0.92` dim on single-image cards as the Cubitts-style "interactive whisper" — premium minimal sites don't zoom; **(b) brand-aligned `*:focus-visible` ring** — 2px solid `currentColor` + 2px offset, no box-shadow. Using `currentColor` instead of a hard-coded hex means the ring inherits whatever text color the scheme uses, so contrast holds on light scheme-1 + dark scheme-3 alike without per-scheme overrides. Dawn's per-component button/field focus styles (more-specific selectors) untouched; **(c) universal `prefers-reduced-motion` killswitch** dropping every `*`-targeted transition + animation to 0.01ms when the OS asks. Stronger than Dawn's `.motion-reduce`-class opt-in approach — that only kills motion on elements that opt in; ours is global, the a11y baseline our quality gates promise (§9). **(4) `/pages/lentile` built (commits 1a87c63 + 962d42a fix).** The homepage now points at `/pages/lentile` twice (lens callout button + multicolumn column 1) — it cannot stay a stub. Created `templates/page.lentile.json` — six sections composing Dawn primitives (rich-text + multicolumn + collapsible-content) into an editorial flow: **hero** (caption / italic heading / body) → **4-up process** (`01 Trimiți rețeta` → `02 Verificăm` → `03 Montăm` → `04 Primești`) → **index explainer** rich-text on shaded scheme-2 ("Lentile subțiri sau standard?" — 1.5 / 1.6 / 1.67 / 1.74 guidance) → **3-up treatments** (antireflex / hidrofob / filtru lumină albastră) → **FAQ collapsible-content** (4 questions: PD, photo of prescription, timeline, online-only) → **CTA** back to `/collections/rame`. Editorial discipline per §6 ("tu" address, comma-below diacritics, no AI-smell) + §7 (educational not medical-claim — every sentence audited). **Returns question deliberately omitted** from FAQ — §7 says don't draft returns policy from memory before Friend's lawyer reviews the custom-made-goods exemption for prescription lenses. Two factual placeholders flagged for Friend's native pass: "5-10 zile lucrătoare" (confirm against his lens-supplier turnaround) and "Un optometrist autorizat verifică rețeta" (confirm wording for his M6 regulatory partnership). **Section-naming bug + fix** (commit 962d42a, saved as `memory/feedback_dawn_section_naming.md`): first push used `"type": "collapsible_content"` (underscore); Shopify section types must match the `.liquid` filename which is hyphenated (`collapsible-content.liquid`). Theme dev returned 500 + "Section type does not refer to an existing section file" in the upload-errors panel. Block types within a section stay underscored (`collapsible_row`). Confused me for ~60 seconds; memory entry written so it doesn't again. **(5) `/pages/despre` built (commit 13749b0).** Created `templates/page.despre.json` — five sections, ~half the length of /lentile: **hero** ("Mai puține rame, alese mai bine.") → **positioning** rich-text on scheme-2 ("De ce așa puține modele") → **3-up "Cum lucrăm"** (selecție / lentile pe rețetă / online) → **place** rich-text (Brașov, online-national) → **CTA**. **Discipline: zero fabricated facts.** Per §11 "ask when unsure rather than guess" — no founding year, no founder names, no specific product count (would break post-supplier-feed), no brand name surfaced inline (rename of placeholder "Optic Mărășești" survives). Content limited to positioning we've established + operational facts I know (Brașov base, online-only, prescription verification). The sharpest sentence is a quiet competitive swipe ("magazinele online de ochelari trăiesc din varietate — mii de rame, sortate după marcă, după trend") — Cubitts-tone, but flagged for Friend's pass: keep or soften. Also flagged: confirmation there's no walk-in to surface, whether Friend wants his name surfaced. **Process learnings:** (a) **Editorial composition of Dawn primitives is the right play** for content pages — no custom sections written, just thoughtful JSON composition. Heroes, process flows, FAQs, CTAs all sit cleanly in rich-text + multicolumn + collapsible-content. Custom sections only earn their place when the schema doesn't fit. (b) The numbered-step pattern (`01 ` `02 ` etc. as title prefix with double-space separator) reused across /lentile process and /despre Cum lucrăm — consistency win or repetition? Question for Stefan's eye on preview. (c) `scheme-2` (shaded surface) used sparingly — only on the middle "explainer" sections of each page — to create vertical rhythm. If it reads as a needless break in preview, flip to scheme-1 in admin Customize. (d) The Recraft research detour was the kind of "is the locked stack still right?" check worth doing periodically — locking a tool 6 months ago doesn't mean it's still best, and 6 commits of cost (one docs commit, no rework) was worth catching a meaningful capability gap. **Validation:** all theme commits validated with `shopify theme check --fail-level=error` — 0 errors, same 12 pre-existing Dawn warnings as sessions 9/10. JSON validated with `python3 -c "json.load(...)"` before each commit. **Admin clicks Stefan needs to make:** (1) Pages → Lentile → Theme template → page.lentile → Save. (2) Pages → Despre → Theme template → page.despre → Save. Without these binds, the new templates exist but the pages still render through the default `templates/page.json`. **Carryovers into next session (priority order):** (1) **Recraft signup + first hero generation pass** — Stefan signs up at recraft.ai free tier, uploads mushroom palette to Brand Kit, uploads Rado True Thinline poster + 2-3 Hodinkee refs to Style Library, generates first batch of frame-exploded hero candidates per the prompt direction in `memory/project_hero_imagery_plan.md`; I rewrite the prompt for Recraft (much shorter than the Flux version since palette + style come from uploads), then wire the winner into `templates/index.json` (replace current type-led rich-text hero). Then lens-exploded for `/pages/lentile`. (2) **PDP polish** — variant picker treatment, image gallery, related-products styling (untouched in M5 so far). (3) **`/pages/contact` editorial pass** — currently using Dawn's `page.contact.json` with stock contact form; could get the same brand-statement intro treatment as /lentile + /despre. **Lower-priority M5 carryovers still pending:** cedilla cleanup (admin punch list in `docs/m5-cedilla-cleanup.md`), replace Universităţii's watermarked iStock image, delete old US dev store `stefan-dev-lens-store`, fix "Stoc epuizat" via inventory tracking, change Shopify currency format `RON` → `lei`. **Friend's native-speaker review pending across:** session 9 product/homepage Romanian copy + session 11 /lentile + /despre copy. **M4 (lens calculator) still blocked on Friend's pricing logic** + lens product-modelling decision (`docs/m3-data-model.md` §10).
- **May 27, 2026 (session 10)** — **M5 polish: nav parents made navigable; cedilla cleanup audited + scoped.** Three commits to `main`. **(1) Cedilla audit + punch list (commit 39fa941, doc only).** Worked the M3 session-7 carryover. Grepped the whole repo for the deprecated cedilla codepoints (`ş` U+015F, `ţ` U+0163, etc.): the **theme is clean** — only `locales/tr.json`/`tr.schema.json` match (Turkish, where cedilla is correct), and `locales/ro.json` (148 comma-below occurrences) plus all session-9 copy are correct. So the cedilla divergence is **entirely admin data** (placeholder products + smart-collection rules), which Claude can't edit (`--allow-mutations` banned, §4). Wrote `docs/m5-cedilla-cleanup.md` — an exact before→after punch list grouped by admin screen. Key engineering point captured: only **s** and **t** carry the cedilla/comma distinction (`ă â î` never affected), and the `bărbați` metafield value + the `rame-barbati` rule must flip **together** because smart-collection matching is codepoint-exact — otherwise `Rame bărbați` empties. Doc also recommends a CSV-export-and-grep pass to turn the few "verify" items into confirmed strings, and a forward prevention note (Friend's real supplier CSV must use comma-below). PLAN item flipped ⬜→🟡 (theme done, admin pending Stefan's clicks). **(2) Desktop dropdown parent navigable (commit 264b7e7).** Data-model §6 carryover. Dawn renders a parent-with-children as a `<summary>` toggle that ignores `link.url`, so `Rame` could only open the dropdown, never reach `/collections/rame` (hence the `Vezi toate` first-child workaround). Confirmed via the CSS that Dawn's desktop dropdown opens on **click, not hover** (`details[open] > .header__submenu`, no `:hover` rule) — which means a naive "make the parent a plain link" would *kill* the dropdown for mouse users. So the fix is a **split control**: wrap the parent label in `<a href="{{ link.url }}">` *inside* the `<summary>` (navigates), leaving the caret as the disclosure toggle. The inner `<span>` is kept so Dawn's hover/active/open underline CSS still targets it; new `.header__menu-parent-link { color: inherit; text-decoration: none }` resets the default link styling. Added `or link.current` to the active state. One honest caveat noted to Stefan: a link nested in a `<summary>` (exposed as a button) is a mild a11y smell (nested interactive), common in production, accepted here over a much larger `<details>`-abandoning rewrite. **(3) Mobile drawer parent navigable (commit 88f4b30).** Checked `header-drawer.liquid`: the mobile parent was *also* a non-navigable toggle (its submenu-panel header is a back-*button*, not a link), so `/collections/rame` was reachable on mobile **only** via `Vezi toate`. Mirrored the desktop split-control: parent label is now an `<a>`, the rest of the row / caret still slides in the submenu panel. Chose this over a baked-in "view all" link partly because Dawn's existing `view_all` RO string is *formal* ("Afișați tot") and would violate §6's "tu" rule. Top-level parents only (our nav has no third level). `.menu-drawer__parent-link` reset added. **Net:** both viewports reach the collection via the parent label, so the admin `Vezi toate` child is now **optional** (left to Stefan — harmless to keep as an explicit dropdown "view all", safe to remove). **Process:** all three theme commits validated with `shopify theme check --fail-level=error` — 0 errors, same 12 pre-existing Dawn warnings as session 9 (the shopify-liquid skill's `validate.mjs` stays unrunnable; theme check is the §4/§9 gate). **Stefan to preview** both viewports via `shopify theme dev` (Liquid/CSS hot-reload, no restart). **Carryovers (priority unchanged):** the locked **frame exploded-view hero** (Stefan runs Flux 1.1 Pro, Claude prompts + wires into `index.json`) per `memory/project_hero_imagery_plan.md`, then the lens exploded for `/pages/lentile`. **Admin punch list pending:** the cedilla fixes in `docs/m5-cedilla-cleanup.md`; plus the older M3 admin carryovers — replace Universităţii's watermarked iStock image, delete old US dev store, fix "Stoc epuizat" via inventory tracking, change currency format `RON`→`lei`. **M4 (lens calculator) still blocked on Friend's pricing logic** + lens product-modelling decision (`docs/m3-data-model.md` §10).
- **May 26, 2026 (session 9)** — **M5 polish continued: storefront now reads Romanian end-to-end, PDP specs are live from metafields.** Two punch-list items shipped, both committed to `main`. **(1) Romanian copy pass (commit 2c0cfba).** Replaced every leftover English placeholder string across `sections/header-group.json` (announcement bar), `templates/index.json` (brand rich-text, featured-collection title, collage heading, 3-column block), and `templates/product.json` (4 accordion titles, image-with-text, 2 columns, related-products heading, share label). Voice per CLAUDE.md §6 — "tu" address, comma-below diacritics (verified no cedilla codepoints via grep), Cubitts-restrained, no AI-smell, no medical claims. **Two judgment calls flagged for Friend's native pass:** the product page's "Free Shipping" / "Hassle-Free Exchanges" columns were **repurposed** to policy-free value props rather than translated, because free shipping isn't a confirmed business decision and exchanges touch §7 returns law (prescription lenses may be exempt as custom-made); and the body filler ("Pair text with an image…") was rewritten as short on-brand draft copy that asserts no unconfirmed specifics. The empty **collage** and random-YouTube **video** homepage sections were left untouched beyond the collage heading — they're structural keep/configure/remove decisions for the imagery/layout pass, not copy. RO is the JSON source; EN comes later via Translate & Adapt. **(2) PDP metafields wired (commit d933695).** Added an opt-in `frame_specs` select (`none`/`materials`/`dimensions`) to Dawn's `collapsible_tab` block in `sections/main-product.liquid`. Chosen over new block types because it reuses Dawn's exact `<details>` accordion markup (styling stays consistent) and over heading-text matching because a real setting survives renames + the EN locale. When set, it renders a `<dl class="frame-specs">` of the relevant `custom.frame_*` fields via `| metafield_text`, each row guarded by `!= blank` so missing data renders nothing. Materiale → frame_material / frame_colour_finish / frame_country_origin; Dimensiuni → frame_lens_width / frame_bridge_width / frame_temple_length / frame_total_width (each `NN mm`, trailing zeros stripped — confirmed via docs search before writing). `.frame-specs` styling in `base.css`: mono labels + values, hairline dividers, echoing the card spec line. `templates/product.json` sets `frame_specs` on the two accordions. Closes the session-8 "I expect to see dimensions here" gap. **Process learnings:** (a) Followed §4 — `shopify-liquid` skill `search_docs.mjs` confirmed `metafield_text` output per type before writing. But the skill's own `validate.mjs` **crashes** (`ERR_MODULE_NOT_FOUND: @shopify/theme-check-common` — plugin cache ships no `node_modules` for the skill scripts), so `shopify theme check --fail-level=error` is the working §4/§9 validation gate (same engine). theme check writes its report to **stderr** — don't `> file` then grep stdout. Saved to `memory/feedback_liquid_skill_validator_broken.md`. (b) theme check clean after both changes: 0 errors, 12 warnings all pre-existing Dawn (incl. the known Google-Fonts RemoteAsset warnings from session 8). **Carryovers into next session (unchanged priority):** the locked **frame exploded-view hero** (Stefan runs Flux 1.1 Pro, Claude prompts + wires into `index.json`) per `memory/project_hero_imagery_plan.md`, then the lens exploded view for `/pages/lentile`. **Lower-priority M3 admin carryovers still pending:** replace Universităţii's watermarked iStock image; delete old US dev store `stefan-dev-lens-store`; fix "Stoc epuizat" via inventory tracking; change Shopify currency format `RON` → `lei`; cedilla→comma-below cleanup across products + collection rules. **M4 (lens calculator) remains blocked on Friend's pricing logic** + lens product-modelling decision (`docs/m3-data-model.md` §10).
- **May 25, 2026 (session 8)** — **M5 opened. M2 design system landed in Dawn; first editorial hero shipped.** Goal of the session: Stefan wanted to see the site rendered with our M2 fonts and theme. The starting state (per session 6's change log) was that design tokens were defined in `:root` but no Dawn sections referenced them — both systems coexisted, only Dawn's was wired up. Shipped three layers: **(1) Fonts + palette plumbing.** Added Google Fonts `<link>` to `layout/theme.liquid` for Source Serif 4 (variable, opsz 8–60, weights 300–600 incl. italic), IBM Plex Sans (300/400/500/600), IBM Plex Mono (400/500) — latin-ext subset auto-included for Romanian diacritics, `display=swap`. Appended `:root` overrides to `assets/base.css` (which loads after Dawn's inline `<style>`, so cascade wins) that rewire `--font-body-family` → IBM Plex Sans and `--font-heading-family` → Source Serif 4. Rewrote all 5 colour schemes in `config/settings_data.json` to the mushroom palette: scheme-1 mushroom default (`#FAF8F3` bg + `#1A1A17` text + dark solid buttons), scheme-2 shaded surface (`#F3EFE5`), scheme-3 dark inverse (`#1A1A17` bg + `#FAF8F3` text — for contrast moments), scheme-4 white card (`#FFFFFF` — image plates), scheme-5 mushroom-taupe accent button (`#8C7A5C` — sparing). **(2) Card + PDP polish to match `docs/m2-kit-page.html` literally.** Edited `snippets/card-product.liquid`: wrapped product title in `<em>` (italic via CSS), inserted spec line block before price render — pulls `custom.frame_material`, `custom.frame_lens_width`, `custom.frame_colour_finish` and joins them with ` · `. Crucial bug fix: first pass used `| append: metafield` which JSON-serialized the Dimension type as `{"value":48.0,"unit":"mm"}` on the storefront — replaced with `| metafield_text` filter (Shopify's purpose-built renderer; formats dimensions as `48 mm` with trailing-zero strip, looked up via Dev MCP). Added `.card-wrapper.product-card-wrapper .card__heading em { font-style: italic }` + `.product-spec { font-family: var(--mono); color: var(--ink-muted); letter-spacing: 0.04em }` + mono on `.price` and `.price .price-item`. Added `.product__title { font-family: var(--serif); font-style: italic }` so PDP title matches card italic, plus mono on PDP `.product .price`. **(3) Hero swap.** `templates/index.json`: replaced `image_banner` section (Dawn's stock cartoon hippies-at-a-lake illustration) with `hero` section of type `rich-text`. Blocks: caption "OPTIC MĂRĂȘEȘTI" (caption-with-letter-spacing, medium) + heading `<em>Rame pentru vedere.</em>` (hxxl) + text "Cinci modele alese cu grijă. Lentile pe rețetă, montate corect." + button "Vezi ramele" → `shopify://collections/rame`. Section: scheme-1 mushroom, full_width, padding 100/100. Added CSS for `.rich-text .caption-with-letter-spacing { font-family: var(--mono); color: var(--ink-muted); letter-spacing: 0.08em; text-transform: uppercase }` so the eyebrow renders as a mono mushroom-muted strap, and `.rich-text__heading em { font-style: italic }` so the inline_richtext italic markup carries. **Process learnings (saved to memory):** (a) `shopify theme dev` only hot-reloads CSS + Liquid; **JSON template/config/locale edits require a full CLI restart** (`Ctrl+C` then re-run). Stefan caught this independently when the caption block didn't appear after first save. Memory: `feedback_shopify_dev_hot_reload.md`. (b) Dev store has a **mandatory storefront password** that can't be disabled until paid plan (M6). `shopify theme dev` prompts for it interactively on each boot — the prompt cannot be satisfied from a Claude Code subprocess or `!` prefix (neither has a real TTY); Stefan must run the CLI in his own terminal window. CLAUDE.md §3 was stale (said "not set") — fixed in this session's docs commit. Memory: `project_dev_store_storefront_password.md`. (c) **Liquid metafield rendering pattern:** always use `| metafield_text` for plain text output of any metafield type. Bare `{{ metafield }}` of a Dimension JSON-serializes; manual `.value.value` + `.value.unit` is verbose and fragile (the inner `.value` returns the Measurement object, not the number — `| round` then coerces to 0). Memory: `feedback_liquid_metafield_rendering.md`. **Validation:** Stefan showed the homepage to his friend and his wife (a web designer); both approved the M2 direction. Establishes baseline taste bar for future polish work. **Decision locked for next session:** Two AI-generated exploded-view hero images, Hodinkee/Rado technical-illustration aesthetic, mushroom monochrome: (1) **frame** exploded on homepage (5–7 components: front, hinges, arms, tips, nose pads, lenses — frame is what customers touch, photographs cleanly, AI handles frame geometry well), (2) **lens** exploded on `/pages/lentile` (substrate + coating layers — earns its place on the lens page; ruled out as homepage hero because lens is emotionally distant, AI struggles with optical-accuracy, less visually varied than a frame). Generation via Flux 1.1 Pro per `CLAUDE.md §1`. Full prompt direction + ranked alternatives in `memory/project_hero_imagery_plan.md`. Provisional — homepage may shift to real product photography once Friend delivers samples. **Carryovers into next session:** (1) Generate first Flux pass of the frame exploded hero, iterate against the brand brief, swap into `templates/index.json` replacing the current rich-text hero. (2) Then start lens exploded for `/pages/lentile`. **Earlier M3 carryovers still pending** (lower priority now that M5 polish has visible momentum): replace Universităţii's watermarked iStock image with a free Pexels/Unsplash equivalent (2 min admin); delete the old US dev store `stefan-dev-lens-store`; fix "Stoc epuizat" on all 5 products via inventory tracking; change Shopify currency format from `RON` → `lei` (admin). **M4 (lens calculator) remains blocked on Friend's pricing logic** + lens product modelling decision per `docs/m3-data-model.md` §10 open questions.
- **May 22, 2026 (session 7)** — **M3 exit criterion met. Sample catalog complete; storefront browsable end-to-end.** Shipped: (1) Verified Mărășești auto-populated all 5 smart collections (1 product each) — full schema validation, list-metafield contains-semantics confirmed working. (2) Created 4 additional placeholder frames via **CSV bulk import** — Aviatorilor (metal/aviator/bărbaţi, 349,00 RON), Universităţii (acetat/cat-eye/femei, 399,00 RON), Victoria (titan/rectangular/bărbaţi, 499,00 RON), Lipscani (acetat/rotund/both-genders, 379,00 RON). Used a Mărășești export as the column-format template; the resulting 4-row CSV imported clean on first try, all 4 products joined the correct smart collections (Toate ramele=5, Rame bărbaţi=4, Rame femei=3, Rame rotunde=2, Rame din acetat=3). Photos uploaded manually per product after import. (3) **Discovered + fixed Dawn parent-menu-link quirk:** Dawn ignores the `Link` field on a menu item when it has children — `Rame` rendered as dropdown-only despite being linked to Toate ramele. Workaround: added `Vezi toate` as first sub-item with the same link target. Documented in `docs/m3-data-model.md` §6. (4) Storefront walk-through validated: `/collections/rame` shows 5-card grid, dropdown nav navigates correctly, Romanian UI strings render throughout (`Filtrează`, `Stoc epuizat`, `Cele mai relevante`, `379,00 RON`), prices formatted with Romanian comma decimal, stock status correct, footer policy links resolve. **Key learnings:** (a) **CSV import is the canonical pipeline for catalog scale.** Export-one-product → use as column-format template → bulk-build CSV → import. Shopify accepted human-readable Category text ("Health & Beauty > Personal Care > Vision Care > Eyeglasses") — no taxonomy IDs required. The same pipeline runs unchanged from 4 placeholders to 100+ products from Friend's supplier feed; this is M3's biggest architectural win. (b) **Cedilla vs. comma-below diacritic divergence — tracked as M5 cleanup.** Mărășești was created session 6 with cedilla forms (`Optic Mărăşeşti`, `bărbaţi`, `Lăţime`) — the deprecated Turkish character forms — instead of CLAUDE.md §6's prescribed comma-below forms (`Mărășești`, `bărbați`, `Lățime`). Smart-collection rules also use cedilla. They render identically in most fonts but are different Unicode codepoints, and Romanian SEO/accessibility readers distinguish them. The 4 new products were imported with cedilla forms to match existing data — fixing this would have required simultaneously updating product titles, vendor names, gender_fit values, AND collection rules. Non-blocking for M3; M5 copy-pass cleanup. (c) **Custom metafields don't auto-display on the product page.** Dawn's product template has static placeholder blocks (Materials, Dimensions, Care Instructions, Shipping & Returns) that aren't connected to our `custom.frame_*` fields. Wiring metafield bindings into Dawn's product section is M5 polish work — likely a custom "Specifications" section that lists all 9 frame_* metafields in an editorial table. (d) **iStock previews carry watermarks.** Universităţii's uploaded image is a watermarked iStock preview, not a free Unsplash photo — visible on the storefront card. Replacement carries to next session. **Process learnings:** (a) When the user wanted "Claude does the data, I do the photos" — CSV import is the legitimate path; `shopify store execute --allow-mutations` remains banned (CLAUDE.md §4). (b) The Shopify Dev MCP skill `shopify-custom-data` is for app developers (TOML/GraphQL workflow), not for CSV import format questions. Fallback to **export-an-existing-product as format template** was the right move and is the general pattern for bulletproof CSV imports. (c) Dawn renders parent-with-children menu items as dropdown-only `<summary>` elements; premium themes like Cubitts patch `header.liquid` to make them navigable. The `Vezi toate` first-child pattern is the Shopify-native workaround until M5 polish. (d) Shopify menus moved to **Content → Menus** sub-nav (formerly under Online Store → Navigation) — data-model doc admin paths updated. **Carryovers into next session:** (1) Replace Universităţii's watermarked iStock image with a free Pexels/Unsplash equivalent (2 minutes — Products → Universităţii → Media → delete + upload). (2) Delete the old US dev store `stefan-dev-lens-store` (still carryover from session 5, task #13). (3) Optionally: draft an M5 punch list for the Dawn theme work — wire `custom.frame_*` metafields into product template, fix cedilla→comma-below diacritics across products and collection rules, patch `header.liquid` for navigable menu parents, translate the "Welcome to our store" announcement bar. **After carryovers close, M4 (lens calculator) is next, gated on Friend's pricing logic + lens product modeling decision** (data model §10 open questions 1–2).
- **May 21–22, 2026 (session 6)** — **M3 structural skeleton up, design vs. structure separated.** Shipped: 5 automated collections with proper handles + smart-collection rules; mid-session schema fix flipping `frame_gender_fit` from Choice list to List of choices because Shopify smart collections don't support mixed AND/OR (commit 2ae3543); 3 stub pages including discovering Dawn's auto-applied `contact` template gives us a free contact form; Romanian policy boilerplate inserted to unblock footer nav link picker; main + footer nav wired per data model §6 (lens-led order, Rame with two nested children); Dawn fork pushed to `lens-store-romanian` and published live (theme ID 155087208621) — previously the store was running Horizon (Shopify's new default), and our local Dawn customizations were invisible; first placeholder frame product **Mărășești** created, validating the full metafield architecture end-to-end (list metafield Pentru accepts bărbați + femei multi-select). **Theme decision reaffirmed: Dawn stays** per CLAUDE.md §1 — Horizon is newer/Shopify's default now, but Dawn wins on LLM reliability, sunk M2 cost, and design-language fit (restrained editorial ≈ Cubitts) for this specific project. **Critical discovery: M2 *visual* design isn't actually visible yet** — tokens were applied to `:root` in session 5 but Dawn's section CSS still uses its own typography and color classes; rewiring is M5 polish work, not M3. **Process learnings:** (a) Shopify menus moved from Online Store to Content sub-nav; (b) Romanian policy URLs (`/policies/*`) need "Insert template" in Settings → Legal before the menu link picker surfaces them; (c) dev stores can't fully remove password protection until on a paid plan — that's a Friend M6 prereq, workaround for dev is admin preview URL bypass; (d) Shopify auto-installs Horizon as default theme on new dev stores in 2025+; (e) smart-collection rules for list metafields show only "is equal to" but the semantics is "contains" (verified by Mărășești being assignable as both bărbați AND femei). **Next session opens with:** (1) verify Mărășești auto-populated all 5 smart collections, (2) create the remaining 4 placeholder frames, (3) delete the old US dev store (carryover task #13), (4) preview the storefront end-to-end with multiple products in collections.
- **May 20, 2026 (session 5)** — **Heavy M3 admin execution.** Shipped: design tokens applied to Dawn `base.css` + fixed a pre-existing `featured-product.liquid` schema-translation error (commit c62b917); `docs/m3-data-model.md` drafted and committed as the schema of record (commit fb691cd); dev store recreated as `lens-store-romanian.myshopify.com` after discovering the original was US-defaulted; Settings → General fully Romanian (business entity, address Brașov, currency RON, metric, grams, Europe/Bucharest); Markets configured Romania-only with RON; Romanian set as primary published language with English added empty; Translate & Adapt installed; **9 frame metafields created** (`custom.frame_*` namespace, 4 Choice list + 1 plain text + 4 Dimension/mm, all with diacritics). **Two big constraints discovered:** (a) EUR display via Shopify Markets requires Shopify Payments configured, which requires Friend's business entity — deferred to M6, captured in CLAUDE.md §1. (b) Dimension-type metafields can't be used in smart collection rules — non-blocking for our 5 starter collections but captured in `docs/m3-data-model.md` §5 so future "narrow frames"-style collections take the tag-based path. **Process learnings:** Shopify Create-store flow ignores country (every new dev store starts US-defaulted, needs Settings → General fix); dev stores live at `dev.shopify.com`, not `partners.shopify.com` — the old "open issue" about Partners visibility was a mental-model error, now resolved in CLAUDE.md §3. **Next session:** automated collections (#6, 5 rules using the Choice list metafields), stub pages (#7 — `lentile`/`despre`/`contact`), navigation (#8 — Lentile in primary slot per lens-led IA), then delete the old US dev store (#13). After M3 admin closes, source Unsplash placeholders and create the first batch of placeholder frame products to verify smart collections auto-populate.
- **May 20, 2026 (session 4)** — **M1 closed, M2 closed in a single session, M3 opened.** Working model clarified: Stefan owns design + product decisions, Friend approves at M5 polish gate (not every milestone). M2 collapsed from 5 sessions to 1 because the joint mood board step became unnecessary. Built four artifacts in `docs/`: reference grid (6 sites scored), typography exploration (3 pairings on Romanian copy), accent exploration (7 warm candidates), kit page (full homepage mock + token reference). **Locked design direction: A/A/D** — serif-led editorial + monochrome with one accent + type-led hero (provisional). **Locked stack:** Source Serif 4 × IBM Plex Sans × IBM Plex Mono, mushroom taupe #8C7A5C accent, 88px display down to 11px mono-xs, 8px spacing base. Closest reference: Cubitts. Full token set committed to `CLAUDE.md` section 5. **Imagery policy clarified:** Unsplash + AI placeholders fine through M5, real product photography required pre-M6 launch; updated section 10 accordingly. Brand name + domain pushed to M5 — building under placeholder "Optic Mărășești." Next: switch from chat to Claude Code, apply `:root` block to Dawn's `base.css`, start configuring collections and metafields.
- **May 19, 2026 (session 3)** — Started M1 step 7 (`CLAUDE.md`). Drafted and committed `docs/GIT_CONVENTIONS.md` first so `CLAUDE.md` can reference it (`docs: add git conventions`). Solo-on-main + Conventional Commits + `--no-ff` for feature branches. **Scope clarification:** site is RO + EN (EN as UX courtesy for tourists/expats physically in Romania, not international storefront) and RON + EUR (RON store/settlement, EUR display-only via Shopify Markets auto-conversion). No EU-wide shipping, no real EUR checkout, no multi-country VAT. M3 picks up Markets config + Translate & Adapt locale setup; M5 copy pass becomes RO primary then EN secondary; M6 SEO adds `hreflang ro-RO` + `en-RO`. `CLAUDE.md` section 1 (Project summary) drafted, awaiting sign-off. Next: section 2 (Repository conventions, points at `docs/GIT_CONVENTIONS.md`).
- **May 18, 2026 (session 2)** — M1 steps 4, 5, 6 done. Local dev env fully wired (Node 20, npm 10, Shopify CLI 3.94.3, Git 2.43), Claude Code 2.1.143 with Shopify AI Toolkit plugin enabled (chose plugin path over raw Dev MCP — auto-updates, bundles skills), telemetry opted out. GitHub auth on this machine via SSH (ed25519). Dawn forked to `stefanptc/eyewear-store` (public), cloned, `shopify theme dev` confirmed working against dev store. **M1 exit criterion met.** Switched from "Claude Code in Cursor terminal" to "Claude Code in standalone terminal" — Cursor optional. Open question for M6: dev store doesn't appear in Partners dashboard, need to resolve ownership before transfer to Friend's account. Next session: M1 step 7 (`CLAUDE.md` in repo root).
- **May 18, 2026** — Plan created. M1 partially done (Partner account + dev store live).
